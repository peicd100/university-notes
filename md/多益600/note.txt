[2026-02-11] 需求摘要
1. 在 `docs\md\多益600` 新增 `轉換.py`。
2. 提供 GUI 設定：語速、每句間隔時間、聲音樣式，並有「轉換」按鈕。
3. 掃描同層所有 `<數字>.md`，僅抽取每行 `<span class="tts">` 後方文字。
4. 目前需求為同時轉換多個檔案，輸出同檔名 `.mp4`，另外產生 `全.mp4`。
5. 所有 `.mp4` 影片畫面需為黑底，中央顯示檔名文字。
5. 使用 workspace root=`docs\md\多益600`，ENV_NAME=`mkdocs`。

[2026-02-11] 實作決策
1. GUI 使用 PySide6，主題色 `#72e3fd`。
2. TTS 使用 edge-tts（免 API key）。
3. 多檔同時轉換：以 asyncio + semaphore 同時啟動全部數字 md。
4. 單句先存暫存 mp3，句間靜音由 ffmpeg 產生，最後 concat 輸出 mp4（AAC）。
5. 產出檔名：
   - `4.mp4`、`5.mp4`、`6.mp4`、`7.mp4`、`8.mp4`
   - `全.mp4`
6. 錯誤處理：
   - 無數字 md 直接失敗。
   - 無 tts 句子的 md 記 warning 後略過。
   - 單句失敗重試 1 次，仍失敗則整批中止。

[2026-02-11] 安裝與調整
1. `conda install -n mkdocs -y -c conda-forge ffmpeg pyside6`
2. `C:\Users\x1064\.conda\envs\mkdocs\python.exe -m pip install edge-tts`
3. ffmpeg 編碼器處理：
   - 句間靜音使用 MP3 encoder（本機實測 `mp3_mf`）。
   - 最終 mp4 使用 AAC encoder（本機實測 `aac`）。
4. 解決過去 UNC/concat 問題：concat 清單改為相對路徑並指定 cwd。
5. 影片畫面使用 ffmpeg `color + drawtext`，黑底 + 置中檔名文字。

[2026-02-11] 驗證命令
1. 語法檢查  
   `C:\Users\x1064\.conda\envs\mkdocs\python.exe -m py_compile docs\md\多益600\轉換.py`
2. CLI 實轉（同時轉換）  
   `C:\Users\x1064\.conda\envs\mkdocs\python.exe docs\md\多益600\轉換.py --workspace docs\md\多益600 --run-once --rate 0 --gap 0.4`
3. ffprobe 驗證  
   `C:\Users\x1064\.conda\envs\mkdocs\Library\bin\ffprobe.exe -v error -show_entries format=duration -of csv=p=0 <檔案>`
4. GUI smoke test（offscreen）  
   啟動 3 秒內未崩潰，確認 GUI 可開啟。

[2026-02-11] 最終結果
1. 轉換成功，已產生：
   - `docs\md\多益600\產生複習音檔\4.mp4`
   - `docs\md\多益600\產生複習音檔\5.mp4`
   - `docs\md\多益600\產生複習音檔\6.mp4`
   - `docs\md\多益600\產生複習音檔\7.mp4`
   - `docs\md\多益600\產生複習音檔\8.mp4`
   - `docs\md\多益600\產生複習音檔\全.mp4`
2. ffprobe 驗證以上檔案可讀且時長 > 0。

[2026-02-14] 功能增強需求
1. 建立新檔案 `紀錄.py`，基於 `轉換.py` 進行增強。
2. 在 `產生複習檔案` 資料夾下建立兩個子資料夾：
   - `一次`：存放原本的音檔轉換結果（<數字>.mp4 + 全.mp4）
   - `兩次`：每句重複兩次，檔名為 <數字>.mp4 + 全.mp4
3. 語速預設改為 0%（1 倍速），用戶可輸入數字調整（-100~100）
4. 聲音樣式預設改為 JennyNeural（en-US-JennyNeural）
5. 加入進度條顯示轉換進度（已通過 GUI 的 status_box 實現）
6. 支持 --mode 參數選擇轉換模式：
   - once：只產生「一次」
   - twice：只產生「兩次」
   - both：同時產生兩者（默認）

[2026-02-14] 實作決策與改動
1. 新增 ConvertOptions.repeat_mode 欄位，支持 "once"、"twice"、"both"
2. 修改輸出目錄邏輯：
   - 原 DEFAULT_OUTPUT_DIR_NAME = "產生複習音檔"
   - 改為 DEFAULT_OUTPUT_DIR_NAME = "產生複習檔案"
   - 自動在下方建立 "一次" 和 "兩次" 子資料夾
3. 新增 _convert_with_mode() 非同步函數，處理單一模式的完整轉換流程
4. 在 extract_tts_sentences 後，若 repeat_sentences=True，則複製每句
5. 修改 convert_markdown_file() 新增 repeat_sentences 參數
6. 調整 convert_workspace() 返回值為 dict，包含 "once" 和 "twice" 的結果
7. GUI 新增兩個 QCheckBox 用戶選擇產生模式
8. PREFERRED_DEFAULT_VOICES 優先級調整為 en-US-JennyNeural 在首位
9. rate_spin 預設值保持 0（代表 0%，即 1 倍速）

[2026-02-14] 實作完成
1. 成功建立 `紀錄.py`，支持所有新需求
2. 更新 README.md：
   - 更新專案用途描述，說明「一次」和「兩次」重複功能
   - 新增程式執行指令（GUI + CLI）
   - 更新打包指令，改為打包 紀錄.py
3. 更新 note.txt，記錄此次修改內容

[2026-02-14] 可用指令
1. GUI 啟動：
   `python 紀錄.py`
2. CLI 純轉換（兩種都產生）：
   `python 紀錄.py --workspace . --run-once --rate 0 --gap 0.4 --mode both`
3. CLI 僅產生「一次」：
   `python 紀錄.py --workspace . --run-once --rate 0 --gap 0.4 --mode once`
4. 指定聲音：
   `python 紀錄.py --workspace . --run-once --voice en-US-JennyNeural --rate 0 --gap 0.4 --mode both`
