a.64 - 16 = 48
b.因為每個 page size = 4KB
c.2^9


先看 CR3 register 找到 P4 起點。
然後透過 P4 index 查找 P4 tabe 找到 P3 index，
然後透過 P3 index 查找 P3 tabe 找到 P2 index，
然後透過 P2 index 查找 P2 tabe 找到 P1 index，
然後透過 P1 index 查找 P1 tabe 找到 frame index(physical frame)，最後 12 bit 是offset
