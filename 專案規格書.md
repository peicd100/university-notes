# 專案規格書

## 基本資訊
- 專案名稱：`university notes`
- 專案類型：MkDocs 靜態網站 / Python 工具專案
- conda 環境：`mkdocs`
- 專案根目錄：`y:\github_note\university notes`
- 正式站點 URL：`https://peicd100.github.io/university-notes/`

## 專案用途
整理大學課程筆記與 blog 內容，使用 MkDocs Material 產出靜態網站，並保留自訂主題、搜尋、RSS、加密頁面與數學公式支援。

## 主要技術
- Python 3.11
- MkDocs
- MkDocs Material
- MkDocs RSS / Tags / Encrypt Content / Offline
- 自訂主題覆寫：`docs/theme/`

## 目錄說明
- `docs/`：Markdown 原始內容、logo、theme 覆寫
- `docs/theme/`：Material 主題覆寫與自訂 CSS / HTML
- `site/`：MkDocs 建置輸出
- `tools/project_launcher.py`：啟動本機靜態站的 Python 啟動器
- `vbs_bat/run.bat`：隱藏啟動流程的批次腳本
- `vbs_bat/university notes.vbs`：不顯示 bat 視窗的入口
- `mkdocs.yml`：網站主設定

## 執行流程
1. 開發預覽：`Y:\conda\envs\mkdocs\python.exe -m mkdocs serve --dirty --livereload`
2. 建置輸出：`Y:\conda\envs\mkdocs\python.exe -m mkdocs build --clean`
3. 隱藏啟動：執行 `vbs_bat\university notes.vbs`
4. 打包前置：先 `mkdocs build --clean`
5. 打包啟動器：使用 `PyInstaller` 打包 `tools\project_launcher.py`

## 目前重要設定
- 主題：`material`
- 自訂主題目錄：`docs/theme`
- 主要首頁入口：`blog/index.md`
- 額外樣式：`assets/pymdownx-extras/extra-8611f6c398.css`、`assets/pymdownx-extras/自定義.css`
- 額外腳本：`assets/pymdownx-extras/extra-loader-iLQ-keay.js`、`assets/pymdownx-extras/toc-fold.js`
- 已啟用功能：navigation tabs、footer、instant、tracking、top、toc.follow、code copy、search.share

## 啟動器規格
- `run.bat` 會先用 `mkdocs build --clean` 更新 `site/`
- 建置成功後，以 `tools\project_launcher.py` 啟動本機 HTTP 服務
- 啟動器預設優先開啟 `blog/index.html`
- 打包時使用 `--add-data "site;site"` 將靜態網站併入可執行檔

## 協作備註
- 此專案從本回合開始正式套用專案規則。
- 專案規則指定維護 `README_PEICD100.md`，因此原本的 `README.md` 保留為舊文件，不作為專案規格主檔。
- GitHub 實際遠端是 `https://github.com/peicd100/university-notes.git`；文件中沿用此實際倉庫位址，以避免 remote 設錯。
- 2026-03-19 已完成手機窄版側欄與首頁卡片 responsive 修正。
- 2026-03-19 已完成手機抽屜標題列 `logo icon` 擋住站名字樣的修正。
- 2026-03-19 已完成 `docs/md/114-2/電機_作業系統/ch2.md` 全檔 Markdown 標題層級下移一級。
- 2026-03-19 已完成 GitHub Pages 首頁跳錯站修正，並同步校正 `site_url`、`robots.txt` 與 TTS localStorage 命名空間。
- 2026-03-19 已完成右側 TOC 摺疊互動，支援 active 路徑自動展開與全部展開 / 全部收合。
- 2026-03-19 已將右側 TOC 的「全部展開 / 全部收合」控制列固定於導覽頂端，方便長目錄捲動時持續操作。
- 2026-03-19 已將右側 TOC 調整為自訂跟隨模式：停用內建 `toc.follow`、保留 sticky 控制列、加入 `自動收合` 切換，並在使用者手動捲動右欄時暫停同步。
- 2026-03-19 已優化右側 TOC 視覺與排版：清除舊的 active 裝飾殘留，改用 grid 安排標題與切換按鈕，降低長標題重疊風險。
- 2026-03-19 已將右側 TOC 欄寬改為以 CSS 變數控制，並將跟隨行為調整為節流更新與最小必要捲動，減少文章捲動時右欄抖動。
- 2026-03-19 已將桌機右側 TOC 的欄寬改為和主區域最大寬度聯動，避免右欄加寬時直接擠窄中間文章；目前章節的高亮與自動跟隨也改為單一 current 來源與置中跟隨策略。
- 2026-03-19 已將右側 TOC 自動收合造成的高度動畫抖動降到最低，並在章節切換後補做一次重新置中，改善 current 項目偏離中央的情況。
- 2026-03-19 已將右側 TOC 控制模式改為 `展開 / 收合 / 自動 / 手動`：展開與收合為一次性操作，會切至手動模式；自動與手動互斥，預設進站為自動模式。
- 2026-03-19 已在窄版改用右下角浮動 `目錄` 按鈕切換右側 TOC 面板，並隱藏主題原本整合到左側抽屜中的 page TOC，避免窄版導覽混入左欄。
- 2026-03-19 已將桌機右側 TOC 欄寬改為視窗寬度導向的動態值：只有螢幕足夠寬才放大右欄，較窄桌機會自動退回原本寬度，以保護中間文章的閱讀寬度與左右留白。
- 2026-03-19 已全面優化右側 TOC 滾動流暢度：以 CSS `overscroll-behavior: contain` 取代 JS wheel preventDefault；摺疊動畫改用 `grid-template-rows: 0fr/1fr`；同步改用 rAF；自動收合僅在章節切換時觸發。
- 2026-03-19 修正右側 TOC 四項問題：側邊欄基礎寬度回歸 12.1rem 不壓內容；控制按鈕 flex 均分加寬；自動摺疊瞬間完成防跳頂；滾動定位即時到位無延遲。
- 2026-03-19 重寫 toc-fold.js 架構：JS 變數取代 DOM 屬性狀態管理；快取 DOM 查詢；移除多層間接包裝；page listener 防重複；支援 instant nav 重初始化。
- 2026-03-19 已將右側 TOC 全面重寫為統一元件：桌機使用 sticky card 式結構，窄版使用右下角按鈕開啟的右側面板；章節追蹤改採 `IntersectionObserver` 驅動並以舒適區方式跟隨，目前閱讀項目只保留單一 current，高亮與收合規則也改為 `自動 / 手動` 兩種明確模式。
- 2026-03-19 已將右側 TOC 的 current 與網址 fragment 統一為單一來源：關閉 Material 內建 `navigation.tracking`，改由自訂 scrollspy 在 current 變更時以 `history.replaceState()` 同步 hash；current 判定線也從視窗比例改為接近頁首的固定 offset。
- 2026-03-19 已再次收斂右側 TOC 樣式：桌機與窄版共用較緊湊的工具列比例、亮色模式改為較實的卡片感、暗色模式保留半透明層次，並已實測亮色/暗色與桌機/窄版。
- 2026-03-19 已補強右側 TOC 的亮色模式收尾：針對過去多段 `.md-sidebar--secondary` 透明背景覆寫造成的窄版文字消失問題，改由新版 `peicd-toc-sidebar` 專用規則強制恢復卡片背景；`本頁目錄` 標題列也補上 `min-width: 0`、單行截斷與較穩定的文字對比設定，避免寬版/窄版都出現擠壓或淡到看不清。
- 2026-03-19 已將 `docs/md/Verilog/學習資源整理.md` 內原有 Markdown 標題層級整體下移一級（`#` 改 `##`，依此類推）。
- 2026-03-19 已將 `docs/md/Verilog/學習資源整理.md` 內有數字的標題格式由 `1. ## 標題` 統一改為 `## 1. 標題`。
- 2026-03-19 已依照提供圖片調整 `mkdocs.yml` 中 `Verilog` 導覽順序，並將該區導覽路徑統一改為較穩的正斜線寫法。
- 2026-03-19 已在 `mkdocs.yml` 補齊 `科技_計算機結構` 導覽項目，並依照提供圖片固定為 `SI 前綴表 -> ch 1 -> ch 2`。
- 2026-03-19 已將 `docs/md/114-2/資工_電腦輔助 VLSI 設計/nmos、pmos.md` 加入 `mkdocs.yml` 導覽，建立 `資工_電腦輔助 VLSI 設計` 群組。
- 2026-03-19 已將 `docs/md/114-2/資工_電腦輔助 VLSI 設計/教材.md` 加入 `資工_電腦輔助 VLSI 設計` 導覽，並排在 `nmos、pmos` 前面。
- 2026-03-19 已依照提供圖片新增 `電機_作業系統` 導覽順序為 `首頁 -> ch 1 -> windows CLI 指令 -> Linux CLI 指令 -> vim -> 子行程`；圖片中的 `2/19作業`、`20260312_HW2` 目前在專案內找不到對應檔案，暫未加入。
- 2026-03-19 已依照提供圖片新增 `機電_數位邏輯實驗` 導覽順序為 `首頁 -> Quartus 創建專案 -> 跑 tb -> 選腳位 -> 上傳電路板`。
